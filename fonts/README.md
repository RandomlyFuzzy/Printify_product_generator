Drop .ttf or .otf font files here. The BookFinisher will auto-detect them and cycle through fonts per page. Each page number gets a different font via round-robin selection.

Fonts are logged to prompts.json under source "BookFinisher" with key "font_selected".
